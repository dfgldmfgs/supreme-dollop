# GitHub Workspace Test Repo

This is a test repository created to verify GitHub Workspace functionality.

## Structure
- `main.py`: A simple Python script to test execution.
- `requirements.txt`: Dependencies (currently empty).
- `.gitignore`: Standard Python ignore file.

## Usage
Run the script:
```bash
python main.py
```
